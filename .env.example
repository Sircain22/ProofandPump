# Optional. Required for real swap history and live tape. Server-side only.
# Obtain from https://dashboard.helius.dev
HELIUS_API_KEY=

# Browser-visible local indexer URL. Never put credentials here.
NEXT_PUBLIC_INDEXER_URL=http://localhost:4000
